Add the ability for players to get damaged.  we want player health state to be networked (ideally we use delta compression so we only network changes in health, and don't add a health message for every network sync message).  add fall damage too; players should get hurt when falling from a high distance (though ensure this is toggleable; we'll later introduce items that lets the player jump high, and we'll want to disable fall damage).  fall damage should trigger if the player hits the ground/environment layer with a certain force.

i want player health status to show underneath remote clients; we need a UI bar underneath their name above their head that shows their current health value.  players hit with rocks, or players hit with golfcarts should take damage (i.e. anything that causes them to go flying causes damage).  implement a red vignette when players take damage (just for the local player taking damage).  this vignette should be configurable (i.e. how visible/drastic it is), and should fade after a few seconds after taking damage.  if a player's health gets below 25%, we show a persistent vignette (make this 25% configurable).

if a player dies, they should ragdoll (ragdolls are currently not setup; if we need some sort of preprocessing, we should do this in the process avatar step).  the local player should see an orbit cam around their dead ragdoll body (a follow 3rd person cam, so if they're falling down a slope, they follow the ragdoll body).  ragdoll does NOT need to be perfectly networked; we can just send a ragdoll rpc method, and network the root position.  a 30 second timer should appear for the local dead player, in which another player can come and revive them (show tooltip).  if they get revived, their health goes back to 50%, and they immediately pop back up to their idle pose.  revive should take 5 seconds (configurable); show a wheel with revive process filling it up, with the seconds countdown inside of the wheel.  if a player is not revived, or they give up, they respawn back at their spawn zone.

we can network health changes as minimally as possible; we don't necessarily need server authoritative health changes if that's no the simplest, most extensible, network minimal approach; we're free to have clients report their own health changes if that's easiest and the least network intensive approach.  priority here is performance and minimal network messages and client consensus on health state.

There are a few places with a //todo to add health changes (in health potions, and in potion brew failure).  ensure we wire those up.